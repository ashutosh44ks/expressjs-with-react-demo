## A simple CRUD app that uses expressjs as backend and reactjs as frontend

### How to run
This app is only locally operatable as of now.
1. clone this repository
2. install node modules for both root and client
3. in root start both servers concurrently using "npm run dev"
4. the frontend is on port 5000 while the backend is on port 3000
